# Place `backup-public-key.asc` here (GPG public key) to enable encrypted
# database dumps. See scripts/backup.sh and docker-compose.yml (`backup` service).
# The private key MUST stay off this host.
