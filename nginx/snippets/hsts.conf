# HSTS — production TLS only. NEVER include this in the self-signed config:
# the browser will remember the HTTPS requirement and refuse to load the site
# with an untrusted certificate on subsequent visits.
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
